# Artifacts

This repository intentionally does not track large or machine-generated artifacts needed for full DDPM training, evaluation, and checkpoint-backed inference.

The tracked result tables and figures under `counterfactual_data_build/outputs/` come from prior notebook runs. They are useful evidence of previous experiments, but they are not enough to run real backend DDPM inference.

## Artifact Inventory

| Artifact | Expected Path | Tracked | Generated By | What Breaks If Missing |
|---|---|---:|---|---|
| Release-aware macro events | `counterfactual_data_build/processed/macro/macro_release_events.csv` | No | `release_aware_macro_loader.ipynb` | Market alignment cannot verify or use release-aware macro events. |
| Daily macro panel | `counterfactual_data_build/processed/macro/macro_release_aware_daily.csv` | No | `release_aware_macro_loader.ipynb` | `market_state_and_alignment_builder.ipynb` cannot build the aligned dataset. |
| Market panel | `counterfactual_data_build/processed/market/market_daily_panel.csv` | No | `market_state_and_alignment_builder.ipynb` | Downstream diagnostics and aligned-data inspection are unavailable. |
| Aligned dataset | `counterfactual_data_build/processed/aligned/aligned_counterfactual_dataset_v1.csv` | No | `market_state_and_alignment_builder.ipynb` | Training prep cannot create DDPM windows. |
| Training windows | `counterfactual_data_build/processed/windows/X_train.npy` and `C_train.npy` | No | `training_prep_for_upgraded_ddpm.ipynb` | DDPM training cannot run. |
| Validation windows | `counterfactual_data_build/processed/windows/X_val.npy` and `C_val.npy` | No | `training_prep_for_upgraded_ddpm.ipynb` | DDPM validation and early stopping cannot run. |
| Test windows | `counterfactual_data_build/processed/windows/X_test.npy` and `C_test.npy` | No | `training_prep_for_upgraded_ddpm.ipynb` | Evaluation cannot compare generated paths against test windows. |
| Scalers | `counterfactual_data_build/processed/windows/scalers.json` | No | `training_prep_for_upgraded_ddpm.ipynb` | DDPM inference cannot convert between scaled model space and return space. |
| Window metadata | `counterfactual_data_build/processed/windows/meta_train.csv`, `meta_val.csv`, `meta_test.csv` | No | `training_prep_for_upgraded_ddpm.ipynb` | Date-level inspection and split traceability are unavailable. |
| Best DDPM checkpoint | `counterfactual_data_build/outputs/checkpoints/conditional_ddpm_upgraded_best.pt` | No | `retrain_upgraded_ddpm.ipynb` | Real DDPM evaluation, live notebook generation, and future backend inference cannot run. |
| Last DDPM checkpoint | `counterfactual_data_build/outputs/checkpoints/conditional_ddpm_upgraded_last.pt` | No | `retrain_upgraded_ddpm.ipynb` | Resume/debug comparison is unavailable. |
| Generated sample arrays | `counterfactual_data_build/outputs/generated_samples/*.npy` | No | `retrain_upgraded_ddpm.ipynb`, `evaluate_upgraded_ddpm.ipynb` | Evaluation cannot be reproduced without regenerating samples. |
| Executed notebooks | `executed_notebooks/*.ipynb` | No | `run_counterfactual_pipeline.py` | Prior cell outputs and run logs are unavailable. |
| RAG vector store | `.rag_store/index.faiss` and `.rag_store/metadata.json` | No | `POST /rag/ingest` | `/rag/query` and `/generate-with-context` fail until ingestion is run. |

## Smoke Artifacts

Smoke artifacts are local validation artifacts written under `counterfactual_data_build/smoke/` by:

```bash
python run_counterfactual_pipeline.py --smoke
```

They are intentionally not tracked. They are smaller than the full research artifacts and are used to prove checkpoint-backed backend inference plumbing.

| Artifact | Expected Path | Tracked | Generated By | What Breaks If Missing |
|---|---|---:|---|---|
| Smoke scalers | `counterfactual_data_build/smoke/processed/windows/scalers_smoke.json` | No | `run_counterfactual_pipeline.py --smoke` | Smoke backend inference cannot unscale generated returns. |
| Smoke condition windows | `counterfactual_data_build/smoke/processed/windows/C_test_smoke.npy` | No | `run_counterfactual_pipeline.py --smoke` | Smoke backend inference cannot construct a condition batch. |
| Smoke checkpoint | `counterfactual_data_build/smoke/outputs/checkpoints/conditional_ddpm_smoke_best.pt` | No | `run_counterfactual_pipeline.py --smoke` | `SMOKE_DDPM_ENABLED=1` cannot run checkpoint-backed inference. |
| Smoke model config | `counterfactual_data_build/smoke/outputs/tables/ddpm_smoke_model_config.json` | No | `run_counterfactual_pipeline.py --smoke` | Smoke backend inference cannot instantiate the smoke model. |
| Smoke generated samples | `counterfactual_data_build/smoke/outputs/generated_samples/ddpm_smoke_generated_unscaled.npy` | No | `run_counterfactual_pipeline.py --smoke` | Smoke sample inspection is unavailable. |

Smoke backend inference is opt-in with:

```bash
SMOKE_DDPM_ENABLED=1 uvicorn backend.main:app --reload
```

This is not the full research DDPM. It is SPY-only, uses the smoke checkpoint, and ignores macro scenario fields such as `inflation` and `interest_rate`.

## Tracked Result Artifacts

The repository does track selected summary artifacts:

- `counterfactual_data_build/outputs/tables/ddpm_upgraded_model_config.json`
- `counterfactual_data_build/outputs/tables/ddpm_upgraded_training_history.csv`
- `counterfactual_data_build/outputs/tables/ddpm_upgraded_training_summary.json`
- `counterfactual_data_build/outputs/tables/scaling_check_summary.csv`
- `counterfactual_data_build/outputs/tables/time_split_summary.csv`
- `counterfactual_data_build/outputs/tables/upgraded_ddpm_corr_distance.csv`
- `counterfactual_data_build/outputs/tables/upgraded_ddpm_vs_gaussian_summary.csv`
- `counterfactual_data_build/outputs/tables/window_array_shapes.csv`
- `counterfactual_data_build/outputs/figures/*.png`
- `counterfactual_data_build/outputs/live_counterfactuals/*.png`

These files document prior notebook runs. They do not replace the missing processed data or checkpoints.

## How To Generate Missing Artifacts

From the repository root, install dependencies and run the notebook pipeline:

```bash
pip install -r requirements.txt
python pipeline_preflight_check.py
python run_counterfactual_pipeline.py
```

For offline package/file checks:

```bash
python pipeline_preflight_check.py --skip-network
```

For RAG artifacts, start the backend and run ingestion:

```bash
uvicorn backend.main:app --reload
curl -X POST http://127.0.0.1:8000/rag/ingest \
  -H "Content-Type: application/json" \
  -d '{"force_rebuild": true}'
```

## Backend Implication

The current FastAPI backend does not require the DDPM artifacts because it uses fallback simulation. A future checkpoint-backed backend inference implementation will require at least:

- `counterfactual_data_build/outputs/tables/ddpm_upgraded_model_config.json`
- `counterfactual_data_build/processed/windows/scalers.json`
- `counterfactual_data_build/outputs/checkpoints/conditional_ddpm_upgraded_best.pt`
- enough processed or live feature data to construct a conditioning window

Until those artifacts exist and backend inference is implemented, `/simulate` remains a demo simulation endpoint.
